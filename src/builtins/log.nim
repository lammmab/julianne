import std/logging

var logger = newConsoleLogger()
logger.levelThreshold = lvlDebug

debug("Starting AST analysis")
warn("Found unused variable ", varName)
error("Parse error at token ", token.text)